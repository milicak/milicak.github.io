# first create a jupyter file in _data
# then convert that file to markdown without input cell
# ipython nbconvert --to markdown --template nocell.tpl Bodrumtsunami_dippings.ipynb


# jekyll build
# jekyll serve
